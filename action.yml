name: 'csv to json'
description: 'Transform data'
inputs:
  token:
    description: 'github token'
    default: ${{github.token}}
runs:
  using: 'node16'
  pre: './src/scripts/fetch_download_csv.js'
  main: './src/scripts/parse.js'
